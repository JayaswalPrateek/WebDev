import User from "./User";

const AboutUS = () => (
    <div>
        <h1>Placeholder for About Us</h1>
        <User name="foo" /> {/* passing props to a class based react component */}
    </div>
);

export default AboutUS;